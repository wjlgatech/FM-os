[pytest]
# Collect only the repo test suite. Skills ship their own runnable scripts under
# skills/*/reference/ (e.g. a test_env.py that sys.exit()s as a CLI gate); those are
# executed via subprocess from tests/, not collected directly by pytest.
testpaths = tests
