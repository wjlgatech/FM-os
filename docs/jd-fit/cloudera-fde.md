# Cloudera — Forward Deployed AI Engineer (Senior/Principal), req 261076

**Verified 2026-08-21.** Apply URL returns HTTP 200 and the Workday CXS API confirms
title *"Forward Deployed AI Engineer (Senior/Principal Level)"*, `jobReqId` 261076,
posted 2026-08-13:

```
https://cloudera.wd5.myworkdayjobs.com/external_career/job/USA--New-York--New-York-City/Forward-Deployed-AI-Engineer_261076-1
```

Workday requires a candidate account, so the submit click is Paul's. Sibling reqs exist
for Austria (261083), Prague (261155), Singapore (261130), Australia (261170) and
Japan/Korea (261171) — **do not apply to more than one**; it is the same team.

- **Resume:** `resumes/cloudera-fde.pdf` · `.docx` · `.md` (also copied to
  `career/PaulWu_Resume_ClouderaFDE.{pdf,docx}`)
- **JD text as scored:** `_cloudera-fde.jd.txt`
- **FM-os jdfit: 94/100** over 9 required capabilities — 8 covered, 1 partial.
- **Comp band:** $198,000–$247,500 base (NY & CA) + corporate incentive plan.

## The five duties → the artifact that already does it

| Cloudera duty | Proof block | The evidence |
|---|---|---|
| Build & prototype full-stack AI apps | `sec_filing_rag`, `procurement_triage` | Two complete take-homes, both live, both gated at 1.0 |
| Partner with strategic customers, prototype → production | `enterprise_eval` | Primary technical interface on the Accenture OpenAI-DSI engagement |
| Shape enterprise AI architecture | `agentic_bpa` | Event-sourced multi-agent BPA engine in production, full audit trail |
| Codify solution patterns into starter kits and playbooks | `fde_os_patterns`, `snowflake_os` | 11 skills + 2 workflows + an 11-tool MCP server, one-command install, three runtimes |
| Multiply impact / enablement | `fde_os_patterns` | Other engineers install and run it; a data-platform curriculum compiled into a STAFF/NOT-YET verdict |

## The one gap, named rather than papered over

jdfit's single 🟡 is **large-scale distributed systems / data infrastructure** — which is
precisely Cloudera's own "you may also have" list (Cloudera platform, Spark, Iceberg,
NiFi, GPU inference optimization). It is the resume's `honest_edge`, unedited.

That gap is already an open FM-os checkpoint, `distributed-infra-partial` (due
2026-08-18, **now past due**). This application is the reason to resolve it — either ship
a legitimate artifact or mark it `wont-build`. It should not stay open and silent.

## Two things worth saying in the screen

1. The procurement-triage golden set's **first run failed** (escalation recall 0.889) and
   exposed an unimplemented policy rule. Lead with that, not with the 1.0s — the failure is
   what makes the 1.0s mean anything.
2. Refusal is a first-class outcome in the SEC agent, including on the assignment's own
   trick question. For a data company selling to regulated enterprises, "the agent knows
   when not to answer" is the whole product.
